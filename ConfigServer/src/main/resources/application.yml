server:
  port: 8085

spring:
  application:
    name: ConfigServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/shubhamithub/Microservice-Configuration.git
          clone-on-start: true
